name: shoplist
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI & Icons
  lucide_icons: ^0.257.0

  # Device Preview für responsives Testing
  device_preview: ^1.3.1

  # Zusatzpakete, kompatibel mit Flutter SDK 3.8.1
  characters: ^1.4.0
  material_color_utilities: ^0.11.1

  # Google Fonts für Inria Sans
  google_fonts: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/Title.png
    - assets/Neutral.png
